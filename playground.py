def find_factors(number):
    cross_list = []
    for x in x_list:
        for y in y_list:
            for z in z_list:
                if x != y != z:
                    cross_list.append(x, y, z)
